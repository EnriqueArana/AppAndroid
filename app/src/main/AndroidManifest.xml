<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sistemas.unc.edu.pe.appplatotipico">
    <!-- Este permiso sirve para leer la unidad de almacenamiento del celular -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppPlatoTipico">
        <activity
            android:name=".ActividadListar"
            android:exported="false" />
        <activity
            android:name=".ActividadAgregar"
            android:exported="false" />
        <activity
            android:name=".ActividadImagenPlato"
            android:exported="false" />
        <activity
            android:name=".ActividadDetallePlato"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>